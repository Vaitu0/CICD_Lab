name: test1

on:
  workflow_dispatch:
    branches:
      - main

jobs:
  test:
    runs-on: self-hosted
    steps:
      - run: echo "Hello, World!"
  docker-build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v3
      - working-directory: ./CICD_Lab/Infrastructure
      - run: docker compose build nginx
      - run: echo "nginx build complete"